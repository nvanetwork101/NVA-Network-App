// This is a simpler ESLint configuration that is more compatible with the current Cloud Functions environment.
module.exports = [
  {
    languageOptions: {
      globals: {
        // Define any global variables your project might use here if needed.
      }
    }
  }
];